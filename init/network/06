The used DNS IP adresses are stored in /etc/resolv.conf

Les adresses IP du serveur DNS utilise sont stockees dans le fichier /etc/resolv.conf

(Watch the E-Learning video / Regarder la video sur l'E-Learning)